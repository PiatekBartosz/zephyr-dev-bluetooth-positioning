services:
  zephyr-dev-bluetooth-positioning:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        ZEPHYR_SDK_VERSION: "0.17.1"
        ZEPHYR_VERSION_TAG: "v3.7.0"
    image: zephyr-dev-bluetooth-positioning:1.0.0
    container_name: zephyr-dev-bluetooth-positioning
    volumes:
      - ./:/workspace

    command: sleep infinity